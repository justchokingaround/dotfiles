colors:
  primary:
    background: "#{{colors16.base00}}"
    foreground: "#{{colors16.base05}}"

  normal:
    black: "#{{colors16.base01}}"
    red: "#{{colors16.base0C}}"
    green: "#{{colors16.base0D}}"
    blue: "#{{colors16.base0F}}"
    magenta: "#{{colors16.base0E}}"
    cyan: "#{{colors16.base0F}}"
    white: "#{{colors16.base04}}"

cursor:
  style:
    shape: Underline

window:
  padding:
    x: 25
    y: 25
  dynamic_padding: true
  opacity: 0.8

font: 
  normal:
    family: "{{font.family}}"

  size: {{font.size}}
  offset:
    x: 0
    y: 0
